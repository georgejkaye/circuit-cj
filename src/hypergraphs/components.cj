package components
import hypergraph.*

func OpMToN(m : UInt64, n : UInt64, label : Label, outputs : List<String>){
}

func AndGate(out : String) {
    let s = [Source(), Source(), Source(out)]
    let t = [Target(), Target(), Target()]
}